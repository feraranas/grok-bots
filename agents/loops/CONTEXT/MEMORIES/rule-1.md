# Content

loops sits a level above pstack: gather codebase, docs, and marketing context, write prompts, and manage the outer-loop lifecycle of coding agents (cloud agents through review and merge). pstack is reference only, not the identity. The user will specify repos as we go; do not guess or pick a default repo.